# (also applies when this template is cloned into a standalone client repo)
node_modules
.next
.env*
!.env.example
tsconfig.tsbuildinfo
lighthouse.json
# CI fixture output — regenerated by pnpm ci:snapshot
ci/content-snapshot.json
public/cms-media/ci-*
